/**
 * Displays a list of the commands inputed by the user
 */
interface CommandHistoryBox {

  /**
   * Adds a command to the history box
   * @param command The command to be added to the box
   */
  public void addCommand(Command command);
}
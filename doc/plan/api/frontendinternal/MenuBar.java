/**
 * The MenuBar is responsible for holding different Selectors (background color,
 * grid setting, turtle image, pen color, language) and opening the command list
 * window.
 */
interface MenuBar {

  /**
   * When the open command list window button is clicked, a command
   * list window will open up.
   */
  public void openCommandListWindow();
}